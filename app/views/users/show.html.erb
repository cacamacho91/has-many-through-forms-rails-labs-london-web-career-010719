<h2> <%= @user.username %> </h2>
📧 <%= @user.email %> <br/><br/>

<h3><%= pluralize(@user.comments.count, 'Comment')%>:</h3>
<% if @user.comments.empty? %>
   - User has not commented yet
<% else %>
  <ul>
    <% @user.comments.each do |comment| %>
      <li>
        <%= link_to comment.post.title, post_path(comment.post) %> -
        <%= comment.content %> (<%= time_ago_in_words(comment.created_at) %>)
      </li>
    <% end %>
  </ul>
<% end %>
